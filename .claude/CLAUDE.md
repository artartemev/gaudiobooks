## Testing
Never run `npm run dev` or any local dev server.
All testing is done via Vercel deployment.
Use `vercel --prod` or push to git to deploy.
Do not start local servers under any circumstances.
